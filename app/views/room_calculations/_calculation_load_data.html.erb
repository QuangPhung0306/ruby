<%= hidden_field_tag :r003_load_input_data, "#{project.room_calculation_input.to_json if project}" %>
<%= hidden_field_tag :r003_load_result_data, "#{project.room_calculation_result.to_json if project}" %>
